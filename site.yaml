# Site configuration
# All values here override the generator's built-in defaults.

site:
  title: "dtupper.com"
  description: "personal website, blog, projects"
  author: "Tupper"
  url: "https://dtupper.com"
  language: "en"
  locale: "en-US"
  nav:
    - label: Home
      url: /
    - label: Blog
      url: /blog/
    - label: Projects
      url: /projects/
    - label: About
      url: /about/

# Directory overrides (all relative to project root, these are the defaults)
# dirs:
#   content: content
#   templates: templates
#   static: static
#   output: output

# Section configuration (merged with defaults)
# sections:
#   blog:
#     date_in_url: true
#     template: post.html
#   projects:
#     template: project.html
#   pages:
#     template: page.html

# Build settings
# build:
#   date_format: long
#   posts_per_page: 10
#   generate_rss: true
